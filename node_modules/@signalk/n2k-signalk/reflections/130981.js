const { skEngineId } = require('../utils.js')

module.exports = [
  {
    node: function (n2k) {
      console.log(n2k.fields);
      return (
        'propulsion.' + skEngineId(n2k) + '.nutrasaltCountdown'
      )
    },
    value: function (n2k) {
      return Number(n2k.fields['countdown'])
    }
  }
]
